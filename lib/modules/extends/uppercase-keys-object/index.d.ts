import { Options } from '../utils';
/**
 * Convert string keys in an object to UPPERCASE format.
 * If `obj` isn't a json object, `null` is returned.
 */
export default function upperKeys(obj: any, opt?: Options): object | null;
